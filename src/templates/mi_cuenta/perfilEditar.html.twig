{% extends '/mi_cuenta/perfil.html.twig' %}

{% block editarperfil %} <div class="editarPerfil"><a href=" {{ path('perfil') }}" style="text-decoration: none; color: white"><p class="volver title">Volver al perfil</p></a></div>  {% endblock %}

{%  block formstart %}
    {{ form_start(form_atributos, {'attr': {'class': 'formulario'}}) }}
{% endblock %}


{# Aqui hacemos un if rapido  en 'value' en una sola linea y ponemos por defecto el valor del usuario si existe en la base de datos sino lo dejamos en blanco #}
{% block nacionalidad %}  {{ form_row(form_atributos.nacionalidad,{ 'label' : 'Nacionalidad','value' : login.atributos and login.atributos.getNacionalidad ? login.atributos.getNacionalidad : '' }) }} {% endblock %}

{% block edad %}  {{ form_row(form_atributos.fecha_nac,{ 'label' : 'Fecha Nacimiento','value' : ''}) }}  {% endblock %}

{% block ojos %}  {{ form_row(form_atributos.ojos,{ 'label' : false , 'value' : login.atributos and login.atributos.getOjos ? login.atributos.getOjos : ''}) }}  {% endblock %}

{% block pelo %}  {{ form_row(form_atributos.color_pelo,{ 'label' : false , 'value' : login.atributos and login.atributos.getColorPelo ? login.atributos.getColorPelo : ''}) }}  {% endblock %}

{% block profesion %}  {{ form_row(form_atributos.profesion,{ 'label' : false, 'value' : login.atributos and login.atributos.getProfesion ? login.atributos.getProfesion : '' }) }}  {% endblock %}

{% block ciudad %}  {{ form_row(form_atributos.ciudad,{ 'label' : false, 'value' : login.atributos and login.atributos.getCiudad ? login.atributos.getCiudad : '' }) }}  {% endblock %}

{% block altura %}  {{ form_row(form_atributos.altura,{ 'label' : false, 'value' : login.atributos and login.atributos.getAltura ? login.atributos.getAltura : ''  }) }}  {% endblock %}

{% block peso %}  {{ form_row(form_atributos.peso,{ 'label' : false, 'value' : login.atributos and login.atributos.getPeso ? login.atributos.getPeso : ''  }) }}  {% endblock %}

{% block sexo %}  {{ form_row(form_atributos.sexo,{ 'label' : false, 'value' : login.atributos and login.atributos.getSexo ? login.atributos.getSexo : ''  }) }}  {% endblock %}

{% block buscaGenero %}  {{ form_row(form_atributos.buscaGenero,{ 'label' : false, 'value' : login.atributos and login.atributos.getBuscaGenero ? login.atributos.getBuscaGenero : ''  }) }}  {% endblock %}

{% block gustos %}  {{ form_row(form_atributos.gustos,{ 'label' : false, 'value' : login.atributos and login.atributos.getGustos ? login.atributos.getGustos|join(', ') : ''  }) }}{% endblock %}

{% block descripcion %} {{ form_row(form_atributos.descripcion,{ 'label' : false, 'value' : login.atributos and login.atributos.getDescripcion ? login.atributos.getDescripcion: '' , 'attr': {'maxlength': 500}, }) }} {% endblock %}

{%  block formend %}
    {{ form_widget(form_atributos.submit, { 'label': 'Save' }) }}
    {{ form_end(form_atributos) }}
{% endblock %}



{% block fotoAjaxOk %}
    <p class="p-1 pe-4 ps-2 text-white mb-auto align-self-end d-none eliminarFoto" >Eliminar</p>

{% endblock %}

{% block fotoAjaxKo %}
    <div class="nofoto w-100 h-100 bg-white rounded d-none align-items-center d-flex flex-column">
        <input style="visibility:hidden;" class="w-100 fileupload" id="fileupload" type="file" name="fileupload" accept="image/x-png,image/gif,image/jpeg" />
        <label for="fileupload" class="btn"><i class="bi bi-plus-square mx-auto mx-auto "></i></label>
    </div>
{% endblock %}

{%  block estiloeditar %}
<style>

        #user_attributes_form_gustos input{
            margin-right: 2px;
        }

        #user_attributes_form_gustos label{
            margin-right: 15px;
        }

        form[name="user_attributes_form"] input{
            max-width: 120px;
        }

        .gridContenido select{
            max-width: 120px;
        }

        .formulario{
            display:grid;
            grid-column:2/5;

        }

        .gustos{
            grid-column:1/5;
            font-size: 24px;
            font-weight: 600;
        }

        .descripcion{
            padding: 10px 15px 10px 0px !important;
        }
        .eliminarFoto{
            background-color: #CE5353;
            border-top-left-radius: 360px;
            border-bottom-left-radius: 360px;
            justify-self: end;
            margin-top: 10px !important;
            cursor: pointer;
        }

        .bi-plus-square{
            font-size: 80px;
            color: #CE5353;
        }
        .bi-plus-square:hover{
            color: grey;
        }
        .nofoto{
            cursor: pointer;
        }

        select{
            max-width: 260px;
            padding: 5px;
            margin-right: 10px;
        }
        select, input{
            margin-bottom: 10px;
            border-radius: 100px;
            outline: none;
            border: none;
            box-shadow: 2px 2px 12px -5px rgb(0 0 0 / 31%);
        }
        input{
            padding-left: 5px;
        }
        textarea{
            margin-bottom: 10px;
            border-radius: 20px;
            outline: none;
            border: none;
            box-shadow: 2px 2px 12px -5px rgb(0 0 0 / 31%);
            padding: 10px;
            width: 90%;
            height: 200px;
        }
        #user_attributes_form_gustos{
            width: 80%;
            display: grid;
            grid-template-columns: 0.1fr 0.5fr 0.1fr 0.5fr 0.1fr 0.5fr;
            grid-gap: 0 10px;
            margin-bottom: 20px;
        }

        form button{
            background-color: #CE5353;
            color: white;
            border: none;
            border-radius: 100px;
            width: 100px;
        }
        form button:hover{
            background-color: darkred;
        }
        .volver{
            padding: 15px 20px;
        }
        .volver:hover{
            background-color: darkred;
            cursor: pointer;
        }
        label{
            font-size: 20px;
            font-weight: 600;
            color: #CE5353;
            margin-right: 10px;
        }
        .title{
            font-size: 32px;
            margin-left: 0!important;
        }
        .atributos{
            font-size: 20px;
        }
        .info{
            grid-column: 1/3;
        }

</style>


{% endblock %}

